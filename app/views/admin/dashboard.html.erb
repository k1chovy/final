<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/simple-line-icons/2.4.1/css/simple-line-icons.min.css">
  <style>
    .card-icon {
      font-size: 4rem;
      margin-bottom: 15px;
      color: #fff;
    }
    .card {
      text-align: center;
      border: none;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
      border-radius: 10px;
    }
    .card-body {
      padding: 30px;
    }
    .icon-bg {
      width: 80px;
      height: 80px;
      margin: 0 auto 20px;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 50%;
      color: #fff;
    }
    .stat-card {
      text-align: center;
      padding: 20px;
      border-radius: 10px;
      background: #f8f9fa;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
    }
    .stat-card .stat-icon {
      font-size: 3rem;
      margin-bottom: 10px;
    }
    .stat-card h3 {
      margin: 10px 0 5px;
    }
    .stat-card p {
      margin: 0;
      color: #6c757d;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-dark bg-dark mb-4">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Admin Panel</a>
      <%= button_to "Logout", admin_logout_path, method: :delete, class: "btn btn-outline-light" %>
    </div>
  </nav>

  <!-- Dashboard Content -->
  <div class="container">
    <h1 class="text-center mb-4">Welcome to Admin Dashboard</h1>
    <p class="text-center mb-5 lead">Manage your application here.</p>

    <div class="row g-4">
      <!-- New Posts -->
      <div class="col-md-4">
        <div class="card bg-primary text-white">
          <div class="card-body">
            <div class="icon-bg bg-dark">
              <i class="icon-pencil card-icon"></i>
            </div>
            <h5 class="card-title">New Posts</h5>
            
            <p class="card-text">Manage and view all posts.</p>
            <%= link_to "Manage Posts", posts_path, class: "btn btn-light btn-custom" %>
          </div>
        </div>
      </div>

      <!-- New Clients -->
      <div class="col-md-4">
        <div class="card bg-success text-white">
          <div class="card-body">
            <div class="icon-bg bg-dark">
              <i class="icon-user card-icon"></i>
            </div>
            <h5 class="card-title">New Clients</h5>
            
            <p class="card-text">Manage user accounts and permissions.</p>
            
          </div>
        </div>
      </div>
    </div>

    <!-- Total Statistics -->
    <div class="row mt-5">
      <div class="col-md-4">
        <div class="stat-card">
          <div class="stat-icon text-primary">
            <i class="icon-pencil"></i>
          </div>
          
          <p>Total Posts</p>
        </div>
      </div>

      <div class="col-md-4">
        <div class="stat-card">
          <div class="stat-icon text-warning">
            <i class="icon-speech"></i>
          </div>
          
          <p>Total Comments</p>
        </div>
      </div>

      <div class="col-md-4">
        <div class="stat-card">
          <div class="stat-icon text-success">
            <i class="icon-user"></i>
          </div>
          
          <p>Total Users</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>